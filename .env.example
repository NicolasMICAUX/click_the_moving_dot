# Example environment configuration for Click the Moving Dot game

# To run WITHOUT Google Cloud services (local mode only):
DISABLE_CLOUD=true

# Google Cloud Configuration (only needed if DISABLE_CLOUD is not set):
# GOOGLE_CLOUD_PROJECT=your-project-id
# GOOGLE_APPLICATION_CREDENTIALS=path/to/service-account-key.json

# Server Configuration:
# PORT=3000

# Usage Examples:
# Local mode (no data saving): npm run start:local
# Cloud mode (with data saving): npm start
# Custom ONNX model: npm run start:onnx -- --onnx=path/to/your/model.onnx
# Local mode with custom ONNX: npm run start:local-onnx -- --onnx=path/to/your/model.onnx
